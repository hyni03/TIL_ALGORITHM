# 2026년 9월 11일 TIL - [LV1] [PCCP 기출문제] 1번 / 동영상 재생기

링크: https://school.programmers.co.kr/learn/courses/30/lessons/340213

<br>

## 문제 설명
당신은 동영상 재생기를 만들고 있습니다. 당신의 동영상 재생기는 10초 전으로 이동, 10초 후로 이동, 오프닝 건너뛰기 3가지 기능을 지원합니다. 각 기능이 수행하는 작업은 다음과 같습니다.

- 10초 전으로 이동: 사용자가 "prev" 명령을 입력할 경우 동영상의 재생 위치를 현재 위치에서 10초 전으로 이동합니다. 현재 위치가 10초 미만인 경우 영상의 처음 위치로 이동합니다. 영상의 처음 위치는 0분 0초입니다.
- 10초 후로 이동: 사용자가 "next" 명령을 입력할 경우 동영상의 재생 위치를 현재 위치에서 10초 후로 이동합니다. 동영상의 남은 시간이 10초 미만일 경우 영상의 마지막 위치로 이동합니다. 영상의 마지막 위치는 동영상의 길이와 같습니다.
- 오프닝 건너뛰기: 현재 재생 위치가 오프닝 구간(`op_start` ≤ 현재 재생 위치 ≤ `op_end`)인 경우 자동으로 오프닝이 끝나는 위치로 이동합니다.

동영상의 길이를 나타내는 문자열 `video_len`, 기능이 수행되기 직전의 재생위치를 나타내는 문자열 `pos`, 
오프닝 시작 시각을 나타내는 문자열 `op_start`, 오프닝이 끝나는 시각을 나타내는 문자열 `op_end`, 
사용자의 입력을 나타내는 1차원 문자열 배열 `commands`가 매개변수로 주어집니다. 

이때 사용자의 입력이 모두 끝난 후 동영상의 위치를 "`mm`:`ss`" 형식으로 return 하도록 solution 함수를 완성해 주세요.

### 제한사항
- `video_len`의 길이 = `pos`의 길이 = `op_start`의 길이 = `op_end`의 길이 = 5
  - `video_len`, `pos`, `op_start`, `op_end`는 "`mm`:`ss`" 형식으로 `mm`분 `ss`초를 나타냅니다.
  - 0 ≤ `mm` ≤ 59, 0 ≤ `ss` ≤ 59
  - 분, 초가 한 자리일 경우 0을 붙여 두 자리로 나타냅니다.
  - 비디오의 현재 위치 혹은 오프닝이 끝나는 시각이 동영상의 범위 밖인 경우는 주어지지 않습니다.
  - 오프닝이 시작하는 시각은 항상 오프닝이 끝나는 시각보다 전입니다.
- 1 ≤ `commands`의 길이 ≤ 100
  - `commands`의 원소는 "prev" 혹은 "next"입니다.
  - "prev"는 10초 전으로 이동하는 명령입니다.
  - "next"는 10초 후로 이동하는 명령입니다.

<br>

## 접근법

이 문제는 string으로 된 시간을 int 로 바꿀 수만 있다면 쉽게 풀 수 있는 문제 같습니다.

근데 '오프닝 건너뛰기' 라는 제약이 좀 생소했는데용, 애매하게 적힌 부분이 있어서 처음에는 +10 (=next) 를 했을 때 오프닝 구간에 포함되면 오프닝을 건너뛰는 형태인줄 알고 로직을 작성했숩니다.

그랬더니 전체 테스트에서 몇개가 틀리더라구요??? <br> 멀까 고민하다가 시작 시점에 이미 오프닝 구간에 들어와 있으면 오프닝을 건너뛰는 것을 보고 (입출력 예 - 3번) <br> 
허음 설마?? 하고 -10 (=prev) 를 했을 때도 적용해주니 다 맞더라구요..

제가 생각하기에 약간 애매한 부분이 있는 것 같습니다.. (문제를 더 열심히 읽어야 하나?? ㅜ-ㅜ)

암튼 해당 부분만 잘 인식하면 시간을 숫자로 바꿔서 계속 +10, -10 해주고, 오프닝 구간 체크와 시작, 끝 체크만 잘해주면 풀 수 있습니다.


<br>

### 해결법

일단 시간을 총 몇초인지 반환하는 로직과, 오프닝인지 확인하는 로직은 자주 쓰이기 때문에 함수로 빼주었습니다.

string 시간 -> 숫자 int
```cpp
#include <sstream> // stringstream 쓰기 위한 라이브러리

int calc_time(const string& time) {
    int m, s;
    char colon; // ':'

    stringstream ss(time);
    ss >> m >> colon >> s;

    return m * 60 + s; // 전체 초 단위로 변경
}
```

오프닝 건너뛰기
```cpp
int skip_opening(int now, int op_s, int op_e) {
    if (op_s <= now && now <= op_e)
        return op_e;

    return now;
}
```

마지막으로 int를 다시 string 시간으로 변환해서 return 해주면 됩니다..!! <br>
여기서 주의할 점은! 6:7 -> 06:07 이런식이여야 하기 때문에 체크를 잘 해줘야 합니다
```cpp
int m = now / 60;
int s = now % 60;

return (m < 10? "0": "") + to_string(m) + ":" + (s < 10 ? "0" : "") + to_string(s);
```
<br>

## 최종 결과


### 최종 코드
```cpp
#include <string>
#include <vector>
#include <sstream>

using namespace std;

int calc_time(const string& time) {
    int m, s;
    char colon;

    stringstream ss(time);
    ss >> m >> colon >> s;

    return m * 60 + s;
}

int skip_opening(int now, int opStart, int opEnd) {
    if (opStart <= now && now <= opEnd)
        return opEnd;

    return now;
}

string solution(string video_len, string pos, string op_start, string op_end, vector<string> commands) {
    string answer = "";
    int now = calc_time(pos);
    int op_s = calc_time(op_start);
    int op_e = calc_time(op_end);
    int video_e = calc_time(video_len);

    now = skip_opening(now, op_s, op_e);

    for (const string& command : commands) {
        if (command == "next")
            now = min(now + 10, video_e);
        else
            now = max(now - 10, 0);

        now = skip_opening(now, op_s, op_e);
    }
    
    int m = now / 60;
    int s = now % 60;
    
    return (m < 10? "0": "") + to_string(m) + ":" + (s < 10 ? "0" : "") + to_string(s);
}
```

### 테스트 결과
<img width="481" height="533" alt="image" src="https://github.com/user-attachments/assets/d38b26e1-442d-4d06-ac48-c26ec2413117" />


<br>


## 후기

문제를 잘 읽어야 겠다.. 생각하게 되었읍니다..
prev에서 오프닝 건너뛰기 상상도 못함;;; 

암튼 오늘도 TIL 완료..!





